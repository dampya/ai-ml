## Notebooks:

1. **Bird Trajectory Prediction:** Training a 2-layer PyTorch RNN on a GPS tracking dataset (Eric, Nico, Sanne) to predict the next latitude and longitude from past movement sequences.

2. **Poem Sentiment Analysis:** Training a BiLSTM on the poems_dataset to classify poems into emotion labels using text preprocessing, stopword removal, tokenization, and model evaluation with accuracy/loss plots.

3. **Chess Outcome Prediction:** Training a CNN on chess positions generated from game move lists. Each position is labeled using Stockfish and the model is trained to predict White’s win probability with validation and checkpointing.

